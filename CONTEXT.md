# 悦读助手

微信读书阅读器的 Chrome 扩展注入脚本，提供宽屏显示、主题颜色、沉浸式阅读、自动阅读、豆瓣搜索联动等功能。

## 核心概念

**悦读助手**：本产品，Chrome Manifest V3 扩展。
_Avoid_: 插件、脚本

**微信读书**：被注入的目标平台（`weread.qq.com`）。

**控制面板**：扩展的悬浮设置面板，鼠标悬停右侧边缘触发。双栏模式下仅显示主题颜色，隐藏宽屏与自动阅读控件。
_Avoid_: 设置面板、侧边栏

**豆瓣搜索联动**：首页搜索框回车后，代理请求豆瓣并展示搜索结果。

## 阅读模式

**滚动模式**：上下滚动阅读，微信读书按钮 class 为 `isNormalReader`。
_Avert_: 单栏模式

**双栏模式**：左右翻页阅读，微信读书按钮 class 为 `isHorizontalReader`。

**宽屏**：将阅读内容区宽度扩展到全宽，正文两侧各留约 12% 视口宽的气口（`WIDE_BODY_MARGIN`，theming.js 的 CSS 层与内联层共用此常量防漂移），通过 CSS 注入 + 内联样式 + MutationObserver 三层实现。仅滚动模式生效。

**沉浸式阅读**：隐藏顶栏/底栏/控制栏，鼠标悬停时显示。当前版本默认开启，无独立状态开关。

## 主题与颜色

**主题颜色**：一套完整的配色方案。主题清单共 16 项（8 浅色 + 8 深色），每项含名称、色值、深浅类型；文字色与次要文字色由深浅类型推导。分为浅色系和深色系两组。
_Avoid_: 背景颜色

**系统默认**：特殊主题（色值为空），浅色/深色清单中各有一个同名条目。选中即撤除插件配色覆盖（清空样式 + 清插件内联文字色），微信读书原生外观接管；像素自愈对其只按方向性亮度判断残留，不做精确色距匹配。
_Avoid_: 原生主题、跟随系统（易与"系统偏好"混淆）

**深浅模式独立记忆**：主题选择按微信读书主题深浅分键持久化（`lightBgIdx` / `darkBgIdx`），各模式记住上次使用的主题；`bgIdx` 是运行时激活索引，不持久化。模式默认 = 该类型清单的首个主题（"系统默认"）。系统深浅切换时恢复目标模式记住的主题，须用事件携带的新模式——过渡期 body class 还是旧值。

**可用主题色列表**：根据微信读书当前主题（深色/浅色）过滤后，当前可用的主题色集合。同名主题（"系统默认"浅深各一）必须按对象身份解析索引，按名称查找恒命中首个。

**微信读书主题**：微信读书当前实际渲染的深色/浅色状态，通过 `body.wr_whiteTheme` class 判断。
_Avoid_: 系统模式、系统深色模式

**系统偏好**：操作系统的 `prefers-color-scheme` 媒体查询值，是微信读书主题变化的触发源之一，但不等于微信读书主题本身。与 body class 观察者构成双信号，先到的信号处理切换、后到的去重跳过。

## 自动阅读

**自动阅读**：自动滚动/翻页功能的总称。仅滚动模式可用，双栏模式下禁用。
_Avoid_: 自动滚动（代码中混用，重构时统一）

**自动模式**：自动阅读的开关，`autoMode === 0` 开启、`=== 1` 关闭。开启后可设置滚动步长/滚动间隔/自动停止，用空格键控制开始/暂停；关闭后自动阅读完全禁用（空格键无效）。

**逐页滚动模式**：自动模式开启时的滚动方式——按步长滚动，到达底部后翻页。

**滚动步长**：逐页滚动模式下每次滚动的像素数。

**滚动间隔**：每次滚动的时间间隔（毫秒）。

**自动停止**：设定 N 分钟后自动停止阅读，0 表示不停止。

**翻页**：到达内容底部后，模拟点击"下一页"按钮或触发键盘事件。

## 微信读书 UI 组件

**顶部栏**：`.readerTopBar`，页面顶部导航。

**底部栏**：`.readerBottomBar`，页面底部区域。

**控制栏**：`.readerControls`，阅读控制按钮组。

**目录面板**：`.readerCatalog`，章节目录侧边面板。

**笔记面板**：`.readerNotePanel`，读者笔记侧边面板。

**AI 问书面板**：`.readerAIChatPanel`，AI 对话侧边面板。

## 微信读书 DOM 容器

**阅读内容区**：`.readerContent`，主内容容器。

**章节内容**：`.readerChapterContent`，章节文字区域。

**渲染容器**：`.renderTargetContainer` / `.renderTargetContent`，文字渲染容器。

**Canvas 容器**：`.wr_canvasContainer`，Canvas 文字渲染区域。

**预渲染容器**：`.preRenderContainer`，预渲染内容区域。

**水平阅读器**：`.wr_horizontalReader`，双栏模式容器。

## 功能机制

**样式注入**：通过动态创建 `<style>` 元素插入 CSS 规则。规则注册表与 DOM 元素分离——微信读书重建 `<head>` 使元素丢失时，下一次注入会全量重写所有规则。

**样式监听**：通过 MutationObserver 监听 style 属性变化，被微信读书覆盖时立即反击。

**Canvas 重渲染（宽屏）**：触发 resize 事件 + display 切换，让微信读书的 Canvas 跟随新容器宽度重绘。

**像素自愈**：系统深浅色切换时，滚动模式 canvas 在 body class 翻转前已按旧文字色画好，之后不再重绘、也不响应 resize/display 切换——CSS 无法修正已画好的位图，唯一可靠修法是读像素分析后直接改写：采样区分纯文字层与整页绘制层（后者只记日志不改写），只重染与旧文字色相近的像素并保留 alpha。

**保护窗口**：系统深浅色切换后约 3.5 秒内，抑制宽屏路径的 Canvas display 切换——此刻微信读书正按新主题重渲染，display 切换会打断它（实测导致文字残留旧主题色）。

**强制重绘（双栏）**：双栏模式切主题后微信读书用旧主题色渲染新 Canvas（微信读书 bug），需延迟 1s 并确认仍为双栏模式后 display 切换强制重绘；延迟是为避开双栏→滚动切换期间模式检测的误判。仅双栏模式使用，滚动模式禁止——微信读书会自己重渲染，打断反而破坏它。

**SPA 导航**：通过劫持 `history.pushState`/`replaceState` 监听微信读书页面内路由变化。

**配置持久化**：通过 `chrome.storage.local` 分键读写用户配置；主题选择按深浅模式独立记忆，旧版单键 `bgIdx` 按主题名迁移后删除。

**页面诊断**：输出 DOM 结构诊断信息到控制台（`window.__wrDiag`）。
