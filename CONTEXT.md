# 悦读助手

微信读书阅读器的 Chrome 扩展注入脚本，提供宽屏显示、主题颜色、沉浸式阅读、自动阅读、豆瓣搜索联动等功能。

## 核心概念

**悦读助手**：本产品，Chrome Manifest V3 扩展。
_Avoid_: 插件、脚本

**微信读书**：被注入的目标平台（`weread.qq.com`）。

**控制面板**：扩展的悬浮设置面板，鼠标悬停右侧边缘触发。
_Avoid_: 设置面板、侧边栏

**豆瓣搜索联动**：首页搜索框回车后，代理请求豆瓣并展示搜索结果。

## 阅读模式

**滚动模式**：上下滚动阅读，微信读书按钮 class 为 `isNormalReader`。
_Avert_: 单栏模式

**双栏模式**：左右翻页阅读，微信读书按钮 class 为 `isHorizontalReader`。

**宽屏**：将阅读内容区宽度扩展到 100%，通过 CSS 注入 + 内联样式 + MutationObserver 三层实现。

**沉浸式阅读**：隐藏顶栏/底栏/控制栏，鼠标悬停时显示。当前版本默认开启，无独立状态开关。

## 主题与颜色

**主题颜色**：一套完整的配色方案，包含背景色、文字颜色、次要文字颜色。分为浅色系和深色系两组。
_Avoid_: 背景颜色

**可用主题色列表**：根据微信读书当前主题（深色/浅色）过滤后，当前可用的主题色集合。

**微信读书主题**：微信读书当前实际渲染的深色/浅色状态，通过 `body.wr_whiteTheme` class 判断。
_Avoid_: 系统模式、系统深色模式

**系统偏好**：操作系统的 `prefers-color-scheme` 媒体查询值，是微信读书主题变化的触发源之一，但不等于微信读书主题本身。

## 自动阅读

**自动阅读**：自动滚动/翻页功能的总称。
_Avoid_: 自动滚动（代码中混用，重构时统一）

**自动模式**：自动阅读的开关，`autoMode === 0` 开启、`=== 1` 关闭。开启后可设置滚动步长/滚动间隔/自动停止，用空格键控制开始/暂停；关闭后自动阅读完全禁用（空格键无效）。

**逐页滚动模式**：自动模式开启时的滚动方式——按步长滚动，到达底部后翻页。

**滚动步长**：逐页滚动模式下每次滚动的像素数。

**滚动间隔**：每次滚动的时间间隔（毫秒）。

**自动停止**：设定 N 分钟后自动停止阅读，0 表示不停止。

**翻页**：到达内容底部后，模拟点击"下一页"按钮或触发键盘事件。

## 微信读书 UI 组件

**顶部栏**：`.readerTopBar`，页面顶部导航。

**底部栏**：`.readerBottomBar`，页面底部区域。

**控制栏**：`.readerControls`，阅读控制按钮组。

**目录面板**：`.readerCatalog`，章节目录侧边面板。

**笔记面板**：`.readerNotePanel`，读者笔记侧边面板。

**AI 问书面板**：`.readerAIChatPanel`，AI 对话侧边面板。

## 微信读书 DOM 容器

**阅读内容区**：`.readerContent`，主内容容器。

**章节内容**：`.readerChapterContent`，章节文字区域。

**渲染容器**：`.renderTargetContainer` / `.renderTargetContent`，文字渲染容器。

**Canvas 容器**：`.wr_canvasContainer`，Canvas 文字渲染区域。

**预渲染容器**：`.preRenderContainer`，预渲染内容区域。

**水平阅读器**：`.wr_horizontalReader`，双栏模式容器。

## 功能机制

**样式注入**：通过动态创建 `<style>` 元素插入 CSS 规则。规则注册表与 DOM 元素分离——微信读书重建 `<head>` 使元素丢失时，下一次注入会全量重写所有规则。

**样式监听**：通过 MutationObserver 监听 style 属性变化，被微信读书覆盖时立即反击。

**Canvas 重渲染（宽屏）**：触发 resize 事件 + display 切换，让微信读书的 Canvas 跟随新容器宽度重绘。

**主题切换与 Canvas**：滚动模式正文由 Canvas 渲染（`.wr_canvasContainer` 内分块 canvas），微信读书切主题后**自己会重渲染**——插件不得强制重绘，display 切换会打断微信读书的重渲染，导致文字残留旧主题色（实测复现路径：深色双栏 → 深色滚动 → 切浅色）。双栏模式微信读书创建新 Canvas 时用旧主题色渲染（微信读书 bug），需**延迟 1s 并在确认当前为双栏模式后** display 切换强制重绘——延迟是为避开双栏→滚动切换期间 `isDoubleColumnMode()` 的误判。

**SPA 导航**：通过劫持 `history.pushState`/`replaceState` 监听微信读书页面内路由变化。

**配置持久化**：通过 `chrome.storage.local` 读写用户配置。

**页面诊断**：输出 DOM 结构诊断信息到控制台（`window.__wrDiag`）。
